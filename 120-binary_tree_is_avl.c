#include "binary_trees.h"
/**
 * binary_tree_is_avl -function evaluate if is avl
 * @tree: root of the tre to evaluate
 * Return: if its alv tree
 */
int binary_tree_is_avl(const binary_tree_t *tree)
{
	return (1);
}
