#include "binary_trees.h"
/**
 * array_to_avl - function create avl tree from array
 * @array: array with nodes
 * @size: size of the array
 * Return: pointer
 */
avl_t *array_to_avl(int *array, size_t size)
{
	return (NULL);
}
