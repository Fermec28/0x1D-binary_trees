#include "binary_trees.h"
/**
 * sorted_array_to_avl -function create avl from array sorted
 * @array: root of the tre to evaluate
 * @size: node to find
 * Return: pointer
 */
avl_t *sorted_array_to_avl(int *array, size_t size)
{
	return (NULL);
}
