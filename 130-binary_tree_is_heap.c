#include "binary_trees.h"
/**
 * binary_tree_is_heap -function check heap tree
 * @tree: root of the tre to evaluate
 * Return: 1 if its heap 0 if not
 */
int binary_tree_is_heap(const binary_tree_t *tree)
{
	return (1);
}
