#include "binary_trees.h"
/**
 * avl_remove -function remove node in a avl tree
 * @root: root of the tre to evaluate
 * @value: node to find
 * Return: pointer
 */
avl_t *avl_remove(avl_t *root, int value)
{
	return (NULL);
}
